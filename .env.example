# Google Maps API Key
# Get your API key from: https://console.cloud.google.com/
# Enable "Maps JavaScript API" in Google Cloud Console
VITE_GOOGLE_MAPS_API_KEY=your_api_key_here

# Backend API Server URL (optional)
# If set, zone data will be saved to the server instead of browser storage
# If not set, the app will use IndexedDB/localStorage for storage
# Example: VITE_API_URL=http://localhost:3001
# VITE_API_URL=http://localhost:3001
